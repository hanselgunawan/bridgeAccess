<div class="col-md-9">
    {{#if itemSearch}}
        <h4><b>Results for</b> {{ searchQuery }}</h4>
    {{else if categorySearchQuery}}
        <h4>{{ categorySearchQuery }}</h4>
    {{else if subCategorySearchQuery}}
        <h4>{{ subCategorySearchQuery }}</h4>
    {{/if}}
    {{#if minPrice}}
        <p><b>Filter Price: </b>₱{{minPrice}} - ₱{{maxPrice}}</p>
    {{/if}}
    <div class="row">
    {{#if itemSearch}}
        {{#each product}}
            <div class="col-sm-3" style="margin-top:30px;">
                <div class="thumbnail"  style="">
                    <center>
                        {{#if ../minPrice}}
                            <a  href="/product_details?id={{ id }}&min={{../minPrice}}&max={{../maxPrice}}&search={{ ../searchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&search={{ ../searchQuery }}">
                            </a>
                        {{else}}
                            <a  href="/product_details?id={{ id }}&search={{ ../searchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&search={{ ../searchQuery }}">
                            </a>
                        {{/if}}
                        <fieldset style="font-size: 12px;">{{ name }}</fieldset>
                        <div class="pull-center">
                            <p class="price" style="font-size: 12px; color: #f39c12;"><b>₱{{ price }}</b></p>
                        </div>
                    </center>
                </div>
            </div>
        {{/each}}
    {{else if categorySearchQuery}}
        {{#each product}}
            <div class="col-sm-3" style="margin-top:30px;">
                <div class="thumbnail"  style="">
                    <center>
                        {{#if ../minPrice}}
                            <a  href="/product_details?id={{ id }}&min={{ ../minPrice }}&max={{ ../maxPrice }}&category={{ ../categorySearchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&category={{ ../categorySearchQuery }}">
                            </a>
                        {{else}}
                            <a  href="/product_details?id={{ id }}&category={{ ../categorySearchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&category={{ ../categorySearchQuery }}">
                            </a>
                        {{/if}}
                        <fieldset style="font-size: 12px;">{{ name }}</fieldset>
                        <div class="pull-center">
                            <p class="price" style="font-size: 12px; color: #f39c12;"><b>₱{{ price }}</b></p>
                        </div>
                    </center>
                </div>
            </div>
        {{/each}}
    {{else if subCategorySearchQuery}}
        {{#each product}}
            <div class="col-sm-3" style="margin-top:30px;">
                <div class="thumbnail"  style="">
                    <center>
                        {{#if ../minPrice}}
                            <a  href="/product_details?id={{ id }}&min={{ ../minPrice }}&max={{ ../maxPrice }}&subcategory={{ ../subCategorySearchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&subcategory={{ ../subCategorySearchQuery }}">
                            </a>
                        {{else}}
                            <a  href="/product_details?id={{ id }}&subcategory={{ ../subCategorySearchQuery }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_details?id={{ id }}&subcategory={{ ../subCategorySearchQuery }}">
                            </a>
                        {{/if}}
                        <fieldset style="font-size: 12px;">{{ name }}</fieldset>
                        <div class="pull-center">
                            <p class="price" style="font-size: 12px; color: #f39c12;"><b>₱{{ price }}</b></p>
                        </div>
                    </center>
                </div>
            </div>
        {{/each}}
    {{else if searchQuery}}
        {{> display_not_found}}
    {{else}}
        {{#each product}}
            <div class="col-sm-3" style="margin-top:30px;">
                <div class="thumbnail"  style="">
                    <center>
                        {{#if ../minPrice}}
                            <a  href="/product_details?id={{ id }}&min={{ ../minPrice }}&max={{ ../maxPrice }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_detail/{{ id }}">
                            </a>
                        {{else}}
                            <a  href="/product_details?id={{ id }}">
                                <img height="130px;" width="130px;" src="{{ smallimage }}" alt="" style="margin-bottom: 0.5cm;" href="/product_detail/{{ id }}">
                            </a>
                        {{/if}}
                        <fieldset style="font-size: 12px;">{{ name }}</fieldset>
                        <div class="pull-center">
                            <p class="price" style="font-size: 12px; color: #f39c12;"><b>₱{{ price }}</b></p>
                        </div>
                    </center>
                </div>
            </div>
        {{/each}}
    {{/if}}
    </div>



</div>